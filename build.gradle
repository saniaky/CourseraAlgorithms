plugins {
    id 'java'
}

dependencies {
    compile 'com.google.guava:guava:23.0'
    compile files('lib/algs4.jar')

    testCompile 'junit:junit:4.12'
}

repositories {
    jcenter()
    flatDir {
        dirs 'lib'
    }
}
